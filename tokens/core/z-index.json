{
  "z": {
    "$description": "Stacking order reference scale, consumed as raw CSS custom properties (e.g. z-index: var(--z-modal)) rather than remapped into a Tailwind utility namespace, so layering stays centralized and collision-free as components are added.",
    "dropdown": { "$value": "1000", "$type": "number" },
    "sticky": { "$value": "1020", "$type": "number" },
    "overlay": { "$value": "1030", "$type": "number" },
    "modal": { "$value": "1040", "$type": "number" },
    "popover": { "$value": "1050", "$type": "number" },
    "tooltip": { "$value": "1060", "$type": "number" },
    "toast": { "$value": "1070", "$type": "number" }
  }
}
